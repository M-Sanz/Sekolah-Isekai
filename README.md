1. install dependency dengan perintah : "composer install"
2. aktifkan server laravel dengan perintah : "php artisan serve"
3. hit api dengan method get ke lokal server dengan route berikut "{host}/api/detail-kelas"
